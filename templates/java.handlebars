import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.remote.RemoteWebDriver;

public class {{title}} {

    private RemoteWebDriver driver;

    public {{title}}(RemoteWebDriver driver){
        this.driver = driver;
    }


    /*PO
    {
        "metadata":"{{{metadata}}}"
    }

    PO*/

    {{#list locators}}
    private By {{name}} = By.{{{locator_type}}}("{{{locator_value}}}");  
      
    {{/list}}

    {{#list locators}}
    public RemoteWebElement get{{{cName}}}(){
        return driver.findElement({{{name}}});
    }      
    {{/list}}
}

