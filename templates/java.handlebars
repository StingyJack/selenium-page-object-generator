public class {{title}} {

    private RemoteWebDriver driver;

    public {{title}}(RemoteWebDriver driver){
        this.driver = driver;
    }

    {{#list locators}}
        private By {{name}} = By.{{locator_type}}("{{locator_value}}");  
      
    {{/list}}

    {{#list locators}}
        public RemoteWebElement get{{name}}(){
            return driver.findElement({{name}});
        }      
    {{/list}}
}

